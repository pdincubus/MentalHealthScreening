---json
{
    "layout": "base.njk",
    "title": "Mental Health Screening"
}
---

<p class="intro">
    These questionnaires are self checks. They do not provide diagnoses.
</p>

<section class="dashboard" aria-labelledby="dashboard-results-heading">
    <h2 id="dashboard-results-heading">Your results</h2>
    <div id="dashboard-results" class="dashboard-results">
        <p>Loading…</p>
    </div>
</section>

<section class="dashboard-all" aria-labelledby="all-tests-heading">
    <h2 id="all-tests-heading">All questionnaires</h2>
    <p>Take a screening test below. Your answers and results are saved in this browser so you can see them on this dashboard.</p>
    <ul class="questionnaire-list">
        {% for questionnaire in questionnaires %}
            <li>
                <a href="/screening/{{ questionnaire.id }}/">
                    {{ questionnaire.title }}
                </a>
                <span class="questionnaire-list__desc">{{ questionnaire.description }}</span>
            </li>
        {% endfor %}
    </ul>
</section>

<script type="module" src="{{ '/js/dashboard.js' | url }}"></script>
